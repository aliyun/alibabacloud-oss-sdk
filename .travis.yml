jobs:
  include:
    - language: node_js
      node_js: 12
      script:
        - cd baseclient/ts
        - npm install
        - npm run ci
      after_success:
        - bash <(curl -s https://codecov.io/bash)
